Next,

Execute the next pending task now in the same turn. Do not only announce, stage, or ask for plan approval.

Use this priority order to choose the task:

1. Continue the current active request if anything remains unfinished.
2. Use `plan.md` for the next tracked item.
3. If `plan.md` is exhausted or unclear, use project memory and recent chat history to recover remaining tasks.

Do one logical task at a time unless the user explicitly asked for a release bundle or “fix all this”. Never repeat or auto-submit multiple Next tasks from a Next button/preset; repeated submissions require the dedicated Repeat `▶ Start` control. When finished, end with a flat numbered remaining-tasks list using simple sequential integers (`1.`, `2.`, `3.`). Keep numbering stable across turns. If nothing remains, state that all tracked tasks are complete.
